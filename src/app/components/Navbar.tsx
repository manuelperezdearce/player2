'use client'

import { Typography, Container, Box } from "@mui/material"

export default function Home() {
    return (
        <Box bgcolor={'red'}>
            <Container >
                <Typography variant="h1" component={'h2'}>Navbar Section</Typography>
            </Container>
        </Box>
    )

}
